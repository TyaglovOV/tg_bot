import TelegramBot = require("node-telegram-bot-api");

export function isAskForWeather(input: TelegramBot.Message) {
  // const splitted = input.text.split(' ')
  //
  // if (splitted[0] === '!погода' || splitted[0] === '!gjujlf') {
  //   return true
  // }

  return false
}
